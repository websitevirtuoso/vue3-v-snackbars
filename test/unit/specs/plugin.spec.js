import { describe, it } from 'vitest';
describe('plugin call functions', () => {
    it.todo("call notification with useNotification('message')");
    it.todo("call notification with notification.success('message')");
    it.todo("call notification with notification.error('message')");
    it.todo("call notification with notification.info('message')");
    it.todo("call notification with useNotification({ title: 'text', color: 'error' })");
});
